# Web 页面/API 访问密钥。公开部署必须改成随机长字符串。
ADMIN_TOKEN=replace-with-a-long-random-secret

# Cloudflare Zero Trust Tunnel 的 token。
# Zero Trust -> Networks -> Tunnels -> Create tunnel -> Docker，复制 token 到这里。
CLOUDFLARE_TUNNEL_TOKEN=replace-with-cloudflare-tunnel-token
