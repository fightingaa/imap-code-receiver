PORT=8787
# 可空。设置后，前端和 API 都需要这个 token。
ADMIN_TOKEN=change-me
# 数据目录。Docker/云部署建议挂载持久卷。
DATA_DIR=./data
# 默认 Microsoft OAuth token endpoint。
MS_TOKEN_URL=https://login.microsoftonline.com/common/oauth2/v2.0/token
